def func():
    while True:
        n = yield
        print(n)


r = func()
r.throw(RuntimeError, "Ошибка")

"""
Операция close() выдает исключение GeneratorExit внутри генератора в текущей команде yield. 
Это приводит к незаметному завершению генератора. 
Если после закрытия генератору будут передаваться дополнительные значения, выдается исключение StopIteration.

Исключения могут выдаваться внутри генератора методом throw(ty [,val [,tb]]), где ty — тип исключения, 
val — аргумент исключения (или кортеж аргументов), а tb — необязательная трассировка:
"""
